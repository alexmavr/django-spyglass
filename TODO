apis:
    test all apis
    reset ApiKeyAuthentication

example:
    allauth: permit user creation without email verification
    profile: add edit view, fix edit/delete buttons
    admin panel: port to spyglass app
    

backend:
    completion notifications (generic message queue vs email)
