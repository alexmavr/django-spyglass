frontend:
    set background, grid relocations, area coloring

spyglass:
    unit test admin_panel

tastypie:
    test all apis

backend:
    completion notifications? (generic message queue vs email)

crawlie:
    test for new content?

deployment:
    set ApiKeyAuthentication
