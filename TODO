example:
    set background, grid relocations, area coloring
    add sample notifications

crawlie:
    test for new content and daemonize

spyglass:
    documentation

deployment:
    set ApiKeyAuthentication
