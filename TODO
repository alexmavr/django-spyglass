frontend:
    set background, grid relocations, area coloring

tastypie:
    test all apis

example:
    allauth: fix login bug (incorrect password/email)
    account profile: add edit query and delete query views
    admin panel: test

backend:
    completion notifications? (generic message queue vs email)

crawlie:
    test for new content?

deployment:
    set ApiKeyAuthentication
